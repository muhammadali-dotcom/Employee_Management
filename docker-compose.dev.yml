services:
  backend:
    volumes:
      - ./BackEnd:/app
      - /app/node_modules
    command: sh -c "npm install && npm run dev"

  frontend:
    volumes:
      - ./FrontEnd:/app
      - /app/node_modules
      - /app/.next
    command: sh -c "npm install && npm run dev"
